<div class="site-footer-wrap">
	<footer class="site-footer" aria-labelledby="site-footer-title">
		<div class="site-footer-inner">
			<div class="footer-heading">
				<p class="eyebrow">Come back soon</p>
				<h2 id="site-footer-title">This place is meant to keep changing.</h2>
			</div>
			<div class="footer-copy">
				<p>
					New thoughts, artsy photos, and strange little creative
					threads will collect here over time. Circle back when you're feeling curious.
				</p>
				<div class="social-links" aria-label="Social links">
					<a href="https://www.instagram.com/itsrandywarner" target="_blank" rel="noreferrer">Instagram</a>
					<a href="https://www.facebook.com/RandyJamesWarner/" target="_blank" rel="noreferrer">Facebook</a>
				</div>
			</div>
		</div>
	</footer>
	<p class="credit">© 2026 Randy Warner. New thoughts pending.</p>
</div>

<style>
	.site-footer-wrap {
		width: min(1180px, calc(100% - 32px));
		margin: 0 auto 56px;
	}

	.site-footer {
		display: grid;
		min-height: clamp(220px, 20vw, 300px);
		padding: clamp(28px, 5vw, 52px);
		border: 3px solid var(--ink);
		background: var(--ink);
		color: #fff9ec;
	}

	.site-footer-inner {
		display: grid;
		grid-template-columns: minmax(0, 0.9fr) minmax(260px, 0.55fr);
		gap: clamp(22px, 5vw, 58px);
		align-items: center;
		align-self: center;
	}

	.eyebrow {
		margin: 0 0 14px;
		font-size: 0.78rem;
		font-weight: 900;
		letter-spacing: 0;
		text-transform: uppercase;
	}

	h2,
	p {
		text-wrap: pretty;
	}

	h2 {
		max-width: 720px;
		margin: 0;
		font-family: Georgia, "Times New Roman", serif;
		font-size: clamp(1.75rem, 3vw, 2.75rem);
		line-height: 1.04;
	}

	.footer-copy p {
		margin: 0;
		font-size: 1.08rem;
		line-height: 1.65;
	}

	.social-links {
		display: flex;
		flex-wrap: wrap;
		gap: 12px;
		margin-top: 20px;
	}

	.social-links a {
		display: inline-flex;
		padding: 8px 0;
		color: inherit;
		font-weight: 900;
	}

	.credit {
		width: 100%;
		margin: 8px 0 0;
		padding-left: 3px;
		color: var(--ink);
		font-size: 0.86rem;
		font-weight: 800;
		opacity: 0.72;
	}

	@media (max-width: 860px) {
		.site-footer-inner {
			grid-template-columns: 1fr;
		}
	}

	@media (max-width: 560px) {
		.site-footer-wrap {
			width: min(100% - 24px, 1180px);
			margin: 0 auto 32px;
		}

		.site-footer {
			min-height: auto;
			padding: 18px;
		}
	}
</style>
